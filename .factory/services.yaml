commands:
  install: bun install
  build: bun run build
  test: bun run test
  lint: bun run lint

services:
  web:
    start: bun run dev
    stop: lsof -ti :4000 | xargs kill
    healthcheck: curl -sf http://localhost:4000
    port: 4000
    depends_on: []
