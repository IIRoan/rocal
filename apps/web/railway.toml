[build]
builder = "RAILPACK"
rootDirectory = "."
buildCommand = "bun install && bun --filter web run build"

[deploy]
startCommand = "cd apps/web && bunx serve out -p $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
