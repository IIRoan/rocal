"use client";

// Browser notifications have been removed from this application.
// This component now returns null since browser notification prompts are no longer needed.
// Email notifications are still available and configured through user settings.

export function NotificationBanner() {
  return null;
}
