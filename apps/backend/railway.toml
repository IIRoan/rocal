[build]
builder = "RAILPACK"
buildCommand = "bun run build"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
startCommand = "./server"

# Health check
[deploy.healthcheck]
path = "/api/health"
timeout = 10
interval = 30
