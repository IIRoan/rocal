[build]
builder = "RAILPACK"
buildCommand = "bun install && bun --cwd apps/backend run build"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
startCommand = "./apps/backend/server"

# Health check
[deploy.healthcheck]
path = "/api/health"
timeout = 10
interval = 30
