{
  "name": "@workspace/notifications",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:emails": "tsx ./email-source/render-emails.tsx",
    "build": "bun run build:emails && go build -o notifications main.go",
    "dev": "bun run build:emails && go run main.go",
    "test": "bun run build:emails && go test ./...",
    "test:coverage": "bun run build:emails && go test ./... -covermode=atomic -coverprofile=coverage.out && go tool cover -func=coverage.out",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "tsc --project tsconfig.json --noEmit",
    "test:mail": "bun run build:emails && go run main.go --test --test-to testmail.jubilant164@passmail.net"
  },
  "dependencies": {
    "@react-email/render": "^1.4.0",
    "@workspace/logger": "workspace:*",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
