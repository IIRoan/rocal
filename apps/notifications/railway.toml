[build]
builder = "RAILPACK"
watchPatterns = ["apps/notifications/**", "bun.lockb"]

[deploy]
startCommand = "bun run prisma:deploy && bun server.ts"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
