[build]
builder = "RAILPACK"
rootDirectory = "."
buildCommand = "bun install && bun run build:notifications"

[deploy]
startCommand = "bun run start:notifications"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
