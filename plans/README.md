# Implementation Plans

Generated by the **improve** skill. Two batches:

- **Backend batch** (001-005): generated 2026-06-10 against commit `92d2488`. Scope: `apps/backend` only. All DONE.
- **Web frontend batch** (006-016): generated 2026-06-10 against commit `3cae505`. Scope: `apps/web` + the web-shared packages (`packages/ui`, `packages/calendar-client`). Focus: performance and look/consistency. All findings were planned per operator instruction.

Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-invite-claim-hardening.md) | Harden invite claim atomicity | P1 | S | — | DONE |
| [002](002-mail-passkey-step-up.md) | Apply passkey step-up to mail secrets routes | P1 | S | — | DONE |
| [003](003-notification-set-transaction.md) | Wrap notification writes in Prisma transactions | P1 | S | — | DONE |
| [004](004-remove-dead-enhanced-notification.md) | Delete unused enhanced-notification-service | P2 | S | — | DONE |
| [005](005-decouple-stalwart-sync-from-list.md) | Stop blocking GET /events on Stalwart sync | P1 | M | — | DONE |
| [006](006-targeted-event-cache-invalidation.md) | Scope event mutation invalidation to affected months | P1 | M | — | TODO |
| [007](007-single-calendar-tree-and-context-memo.md) | Mount one calendar tree per viewport; memoize data context | P1 | M | — | TODO |
| [008](008-month-view-event-bucketing.md) | Memoize month-view per-day event buckets | P1 | M | — | TODO |
| [009](009-bundle-and-asset-diet.md) | Cut initial bundle/asset weight (gsap, pdfjs, images) | P1 | M | — | TODO |
| [010](010-mail-list-render-cost.md) | Reduce mail list render cost (Set hoist, row memo, windowing) | P2 | M | — | TODO |
| [011](011-remove-getevents-retry-hack.md) | Replace getEvents refetch-on-bad-dates with date coercion | P2 | S | — | TODO |
| [012](012-parallelize-mail-startup.md) | Parallelize and cache mail startup fetches | P2 | M | — | TODO |
| [013](013-vault-kdf-off-main-thread.md) | Run mail vault argon2id KDF in the crypto worker | P2 | M | — | TODO |
| [014](014-split-use-mail-app-hook.md) | Split use-mail-app god-hook (compose state first) | P3 | L | 012 | TODO |
| [015](015-semantic-status-colors.md) | Replace raw Tailwind status colors with semantic tokens | P3 | S | — | TODO |
| [016](016-calendar-manager-focus-visible.md) | Restore visible keyboard focus in calendar-manager rows | P3 | S | — | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` | `REJECTED`

## Dependency notes (web batch, 006-016)

- **014** depends on **012** (012 converts the config/status loaders to React Query; 014 restructures the same hook and assumes that shape). **010** also touches mail rendering — land 010 and 012 before 014 to avoid merge churn in `use-mail-app.ts` / `mail-app.tsx`.
- **007**'s context-memo half makes downstream memoization effective; landing it before **008** maximizes 008's payoff, but they are technically independent.
- **006**, **009**, **011**, **013**, **015**, **016** are fully independent and can run in any order or in parallel worktrees (009 and 015 both touch `apps/web/app/login/_content.tsx` — avoid running those two concurrently).

## Findings considered and rejected (web batch)

- **PERF-13 (O(n²) timeline overlap counting, `packages/ui/src/components/calendar/timeline-layout.ts:225`)**: real but memoized upstream by week view and negligible below ~20 events/day; fold into future calendar-layout work, do not fix standalone.
- **PERF-11 (calendar E2EE decryption worker offload)**: needs profiling first; Plan 006 removes most repeated decryption, which likely eliminates the symptom. Re-audit after 006 lands.
- **Gradients/backdrop-blur on home/login/privacy/reset-password**: consistent, intentional page-shell pattern across those surfaces; not a violation worth churning.
- **`mail-avatar.tsx` raw color palette**: deliberate deterministic per-sender avatar colors; keep.
- **`sonner.tsx` raw status colors**: shared-package toast styling; converting affects all consumers — needs a design decision, not a mechanical fix.
- **`debug/mails/page.tsx` hex colors**: email-template preview HTML; literal colors are the point.

## Backend dependency notes

- **001** and **002** are independent; both are security fixes and can land in parallel.
- **003** is independent; safe to run before any event/recurrence refactors.
- **004** should land before **ARCH-02** (EventService split) so reviewers are not confused by the dead module.
- **005** does not depend on other plans; coordinate with mail-realtime sync paths so Stalwart data still converges.

## Findings considered and rejected

- **SECURITY-03 (unauthenticated invite claim)**: Claim-before-sign-up appears intentional — `account-public.ts` exposes claim without auth so users pick a handle before Better Auth account creation. Hardening needs a product decision (signed nonce from invite email), not a blind auth requirement. Partially mitigated by **001**.
- **SECURITY-04 (server-derived vault key)**: Real risk, but removing `derivedKeyB64` breaks native Hermes clients until a WASM argon2id path exists. Deferred — needs native coordination, not a backend-only quick fix.
- **DIRECTION-01 citing `enhanced-notification-service.ts`**: That file is dead code (**004**). Browser notifications are persisted by the backend but the Go worker only dispatches `notification_type = 'email'` (`apps/notifications/main.go:317`). Direction finding reframed below in the audit report.
- **PERF-04 (batched sync-log delete)**: Valid quick win but lower leverage than security/correctness items; not auto-planned.
- **Subscription SSRF / DNS rebinding**: URL validation exists in `subscription.service.ts`; rebinding on first resolve not verified — investigate separately if subscriptions fetch untrusted URLs in production.
